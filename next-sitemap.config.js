// next-sitemap.config.js
module.exports = {
  siteUrl: 'https://four-seasons-cms.vercel.app/', // ← change this to your actual domain
  generateRobotsTxt: true,            // ← optional but recommended
  sitemapSize: 5000,                  // split sitemap if over 5k links
}